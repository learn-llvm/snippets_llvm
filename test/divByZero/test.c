#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {
  int v = 3;
  /// int sum = atoi(argv[1]);
  int sum = rand();
  // int z = 0;
  // int sum = v / z;
  sum = (v / sum);
  return sum;
}
